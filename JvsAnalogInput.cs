using System;

namespace AMDaemon
{
	public sealed class JvsAnalogInput
	{
		//private IntPtr Pointer { get; set; }

		//public int ValidBitCount => Api.Call(Pointer, Api.JvsAnalogInput_getValidBitCount);

		//public int ChannelCount => Values.Count;

		//public LazyCollection<ushort> Values { get; private set; }

		//internal JvsAnalogInput(IntPtr pointer)
		//{
		//	Pointer = pointer;
		//	Values = new LazyCollection<ushort>(() => Api.Call(pointer, Api.JvsAnalogInput_getChannelCount), (int index) => Api.Call(pointer, index, Api.JvsAnalogInput_getValue), false);
		//}
	}
}
