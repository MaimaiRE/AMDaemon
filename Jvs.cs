namespace AMDaemon
{
	public static class Jvs
	{
		//public static bool IsAvailable => Api.Jvs_isAvailable();

		//public static int NodeCount => Nodes.Count;

		//public static LazyCollection<JvsNode> Nodes { get; private set; }

		//static Jvs()
		//{
		//	Nodes = new LazyCollection<JvsNode>(Api.Jvs_getNodeCount, (int index) => new JvsNode(Api.Call(index, Api.Jvs_getNode)), true);
		//}
	}
}
