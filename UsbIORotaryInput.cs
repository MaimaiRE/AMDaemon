using System;

namespace AMDaemon
{
	public sealed class UsbIORotaryInput
	{
		//public int ChannelCount => Values.Count;

		//public LazyCollection<ushort> Values { get; private set; }

		//internal UsbIORotaryInput(IntPtr pointer)
		//{
		//	Values = new LazyCollection<ushort>(() => Api.Call(pointer, Api.UsbIORotaryInput_getChannelCount), (int index) => Api.Call(pointer, index, Api.UsbIORotaryInput_getValue), false);
		//}
	}
}
